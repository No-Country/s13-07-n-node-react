
export class InstructorsController {
   index( req, res ) {
      res.json({message: 'no instructors'})
   }
   show( req, res ) {
      res.send( 'wip')
   }
   create( req, res ) {
      res.send( 'wip' )
   }
   update( req, res ) {
      res.send( 'wip' )
   }
   destroy( req, res ) {
      res.send( 'wip' )
   }
}